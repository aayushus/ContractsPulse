# Database Settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=contractspulse

# API URLs used internally by Docker
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
REDIS_URL=redis://redis:6379/0

# LLM Keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional model overrides (pydantic-ai model ids)
OPENAI_MODEL_EXTRACTOR=openai:gpt-4.1-mini
OPENAI_MODEL_RISK=openai:gpt-4.1

# Security & Signup Settings
DISABLE_SIGNUP=false
JWT_SECRET=super-secret-contractspulse-key-change-it
CORS_ORIGINS=http://localhost:5173,http://localhost:8000

